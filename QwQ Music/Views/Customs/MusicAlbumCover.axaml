<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:QwQ_Music.UI.Behaviors"
        xmlns:customs="clr-namespace:QwQ_Music.Views.Customs">
    <Style Selector="customs|MusicAlbumCover">
        <Setter Property="Template">
            <ControlTemplate>
                <Border
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    Margin="{TemplateBinding Margin}"
                    Padding="0"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{TemplateBinding CornerRadius}"
                    behaviors:DynamicCornerBehavior.IsEnabled="{TemplateBinding IsAutoRoundedCorners}">
                    <Border
                        ClipToBounds="True"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        Margin="{TemplateBinding Padding}"
                        Padding="0"
                        BorderThickness="0"
                        behaviors:DynamicCornerBehavior.IsEnabled="{TemplateBinding IsAutoRoundedCorners}">

                        <!--  专辑封面 Image 控件  -->
                        <Image Source="{TemplateBinding Thumbnail}" Stretch="UniformToFill" ClipToBounds="True" />
                    </Border>

                </Border>
            </ControlTemplate>
        </Setter>
    </Style>
</Styles>